Dmitrii Beliaev

libraries used:
pyodbc, random, string

Application requires them to run properly. File containing them are included in project folder, they can also be installed using pip install command

Application is run with .py file and actions are done all through user commands in terminal.

Application requires access to university-provided DB (which unfortunately is inaccessible as of the end of Spring 2023 Term)
